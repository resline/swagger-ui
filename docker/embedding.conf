#
# Additional embedding restrictions (X-Frame-Options handled in main config)
# This provides additional CSP frame-ancestors enforcement for embedding-specific locations
#
add_header 'Content-Security-Policy' "frame-ancestors 'none'" always;
