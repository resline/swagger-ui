# Example Environment Configuration for Swagger UI with Secure CORS

# Basic Configuration
PORT=8080
BASE_URL=/

# CORS Configuration (SECURITY CRITICAL)
# Enable CORS functionality
CORS=true

# Allowed Origins for Cross-Origin Requests
# IMPORTANT: Replace these with your actual domains
# Format: comma-separated list of full URLs including protocol
# Examples:
#   Production: ALLOWED_ORIGINS="https://myapp.com,https://api.myapp.com"
#   Development: ALLOWED_ORIGINS="http://localhost:3000,http://localhost:8080,https://dev.myapp.com"
#   Same-origin only: ALLOWED_ORIGINS="" (or comment out)

# Production example (replace with your domains)
ALLOWED_ORIGINS="https://myapp.com,https://api.myapp.com,https://admin.myapp.com"

# Development example (uncomment for local development)
# ALLOWED_ORIGINS="http://localhost:3000,http://localhost:8080,http://localhost:9000,https://dev.myapp.com"

# Same-origin only (most secure - comment out ALLOWED_ORIGINS or leave empty)
# ALLOWED_ORIGINS=""

# Other Configuration Options
SWAGGER_JSON_URL=https://api.myapp.com/swagger.json
EMBEDDING=false

# IPv6 Support (optional)
# PORT_IPV6=8080

# Security Headers are enabled by default in the nginx configuration
# The following security features are automatically applied:
# - Strict-Transport-Security (HSTS)
# - X-Frame-Options: SAMEORIGIN
# - X-Content-Type-Options: nosniff
# - X-XSS-Protection: 1; mode=block
# - Content-Security-Policy with restrictive defaults
# - Rate limiting on API endpoints